product:
  type: lib
  platforms: [ iosArm64, iosSimulatorArm64, iosX64, android ]

settings:
  compose: enabled
  kotlin:
    serialization: json

dependencies:
  - ../core
  - $compose.foundation
  - $compose.material3
  - $libs.decompose: exported
  - $libs.compose.extensions.jetbrains: exported
